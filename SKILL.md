---
name: back-to-file
description: 管理互联网项目开发全流程的 Skill，通过六个阶段的文档驱动项目从 idea 到交付。当用户表达任何项目开发意图时触发——"我想做个网站""帮我规划项目""开始一个新项目""梳理一下这个 idea""帮我拆分任务""管理开发进度""做项目计划"。涵盖概念定义(Concept.md)、项目规划(Plan.md)、实施拆分(Project.md)、任务跟踪(Task.md)、实现执行(界面骨架→后端→交互集成→部署)、变更管理(Change.md)和项目总结(Summary.md)。支持不同 Agent 通过读写文档接力完成。
---

# Project Flow - 项目流程管理

通过分阶段文档管理项目开发流程。每个阶段生成一份 Markdown 文档放在项目根目录，不同 Agent 可以通过读取这些文档了解当前状态并继续推进。

## 运行时兼容性

此 SKILL.md 兼容所有支持 Agent Skills 标准的 AI 编码助手。

### 安装方式

**一键安装**（推荐，自动检测运行环境）：
```bash
npx skills add back-to-file
```

**手动安装**：根据你的平台选择对应路径：

| 平台 | 安装路径 |
|------|---------|
| Claude Code | `~/.claude/skills/back-to-file/SKILL.md` |
| Codex CLI | `~/.agents/skills/back-to-file/SKILL.md` |
| Codex App | 项目根目录 `AGENTS.md` |

> Codex App 无内置 SKILL.md 系统，使用仓库内附的 `AGENTS.md`（见本技能目录下的 AGENTS.md）

**工具调用说明**：本技能的工作流指令使用平台无关的语言描述（如「创建文件」「读取文档」），具体在实现阶段由各平台自动映射到对应工具。无需手动区分平台命令。

**依赖技能**：阶段五·前端骨架中的视觉 Demo 阶段依赖 **design-taste-frontend**（taste-skill）提供反 slop 设计规则和三旋钮系统：
```bash
npx skills add https://github.com/Leonxlnx/taste-skill
```

## 触发条件

检测到用户表达以下意图时立即使用此 Skill：
- 开始新项目（"我想做个...", "帮我搭一个..."）
- 项目规划（"帮我规划项目", "写项目计划"）
- 需求梳理（"帮我理清这个 idea", "分析一下需求"）
- 任务拆分（"把项目拆成任务", "分阶段实施"）
- 进度管理（"跟踪项目进展", "管理开发进度"）

即使表达不完整（如"我有一个想法"、"帮我做个东西"），也应该触发。

## 工作流概览

```
用户表达意图
    │
    ▼
阶段一：概念定义 → Concept.md
    │
    │  ← 🔴 CHECKPOINT：交叉校验 Concept.md 覆盖 6 个问题？
    │  ← 🛑 STOP：展示文档 → 请求用户确认 → 等待「进入下一阶段」指令
    ▼
阶段二：项目规划 → Plan.md
    │
    │  ← 🔴 CHECKPOINT：校验 Plan.md → Concept.md（问题覆盖/洞察体现/方案一致）
    │  ← 🛑 STOP：展示文档 → 请求用户确认 → 等待指令
    ▼
阶段三：实施拆分 → Project.md
    │
    │  ← 🔴 CHECKPOINT：校验 Project.md → Plan.md（功能覆盖/技术栈一致/形态匹配）
    │  ← 🛑 STOP：展示文档 → 请求用户确认 → 等待指令
    ▼
阶段四：任务跟踪 → Task.md
    │
    │  ← 🔴 CHECKPOINT：校验 Task.md → Project.md（任务一致/依赖满足/阶段状态）
    │  ← 🛑 STOP：展示进展 → 请求用户确认 → 等待指令
    ▼
阶段五：实现执行
    ├── 5.1 前端（界面骨架）
    │   ├── Design.md（设计系统规范）
    │   │  ← 🛑 STOP：展示文档 → 请求确认 → 等待指令
    │   └── 视觉 Demo/原型 → 对齐 Design.md
    │      ← 🛑 STOP：展示进展 → 请求确认 → 等待指令
    ├── 5.2 后端
    │   ├── DataModel.md（数据模型）
    │   │  ← 🛑 STOP：展示文档 → 请求确认 → 等待指令
    │   ├── API.md（API 设计）→ 对齐 DataModel.md
    │   │  ← 🛑 STOP：展示文档 → 请求确认 → 等待指令
    │   └── 业务逻辑实现 → 对齐 DataModel.md + API.md
    │      ← 🛑 STOP：展示进展 → 请求确认 → 等待指令
    ├── 5.3 前端交互集成
    │   └── 交互实现 → 对齐 Design.md + API.md
    │      ← 🛑 STOP：展示进展 → 请求确认 → 等待指令
    └── 5.4 部署
        ├── CI/CD 配置
        │  ← 🛑 STOP：展示进展 → 请求确认 → 等待指令
        └── 上线
    ▼
阶段六：项目总结 → Summary.md
```

**核心原则**：每个阶段完成后，先做交叉校验（回顾前序文档确保内容对齐），再把文档展示给用户确认。确认后提示用户手动触发下一阶段——这样不同 Agent 可以随时接手。

## 阶段一：概念定义 → Concept.md

**目标**：理清项目的核心想法和概念，确保方向和需求清晰。

### 执行方式

**场景 A：用户提供想法但无文档**
通过对话引导用户回答以下问题，然后将答案写入 Concept.md：

1. **动机** — 为什么想要做这个项目？
2. **问题** — 想要解决什么具体问题？
3. **方案** — 打算用什么方式解决？
4. **理由** — 为什么选择这种方式？
5. **竞品分析** — 是否已有类似方案？它们有什么不足或问题？
6. **深层洞察** — 关于需求还有什么更深入的思考？

**场景 B：用户提交了 Concept.md（或类似文档）**
检查文档是否完整覆盖上述 6 个问题。如有缺失或不明确之处，向用户指出并补充完善。

### Concept.md 模板

```markdown
# [项目名称] - 概念定义

## 动机
<!-- 为什么想做这个项目 -->

## 要解决的问题
<!-- 具体要解决什么问题 -->

## 解决方案
<!-- 打算用什么方式解决 -->

## 方案理由
<!-- 为什么选择这个方案 -->

## 竞品/现有方案分析
<!-- 已有方案及其不足 -->

## 深层洞察
<!-- 对需求的深入思考 -->

## 待定信息
<!-- 以下内容在概念阶段暂不确定，将在项目规划阶段进一步明确 -->
- **产品形态**：Web 应用 / 移动端 / CLI / 库？
- **技术方向**：前后端框架倾向？
- **界面预期**：是否需要用户界面？有多少个主要页面？
```

完成后，输出文档内容并告知用户确认。提示：「请确认 Concept.md 的内容。确认后告诉我「进入下一阶段」，我会继续生成 Plan.md。」

> **下一阶段（Plan.md 生成后）需要校验的内容**：
> - Concept.md 中定义的每个**要解决的问题**，Plan.md 是否有对应的功能或方案来回应？
> - Concept.md 中的**深层洞察**是否在 Plan.md 的设计中得到体现？
> - **方案理由**和 Plan.md 中选用的技术方案是否一致？
> - Concept.md 中的**待定信息**是否已在 Plan.md 中得到确认和明确？

---

## 阶段二：项目规划 → Plan.md

**目标**：基于 Concept.md，生成完整的项目规划文档。这是给后续阶段和开发者看的完整蓝图，需要足够详细。

**前置条件**：项目根目录存在完整的 Concept.md。如不存在，返回阶段一。

**信息补齐**：生成 Plan.md 前，先检查 Concept.md 中的"待定信息"章节。如有未确定项，主动向用户提问补充，再写入 Plan.md。

### Plan.md 模板

```markdown
# [项目名称] - 项目规划

## 项目概述
<!-- 一句话描述项目 -->

## 产品形态
<!-- 是什么类型的项目？Web 应用/移动端/CLI 工具/库? -->

## 核心功能
<!-- 功能的列表，每个功能附带简要描述 -->

## 页面/界面结构
<!-- 有哪些页面/界面，它们之间的关系 -->

## 信息结构
<!-- 数据模型、核心数据结构、存储方式 -->

## 技术栈
<!-- 前端/后端/数据库/部署等 -->

## 设计规范
<!-- 设计风格、UI 规范（如果有） -->
```

- 需要判断产品形态来决定模板中哪些部分是必须的。例如 CLI 工具不需要"页面结构"，但需要"命令结构"。
- 对于 Web 应用：页面结构、组件树、路由设计要详细。
- 如 Concept.md 中有不清晰之处，主动向用户提问补充。

### 对齐校验：Plan.md → Concept.md

生成 Plan.md 后，对照 Concept.md 逐一检查：

1. **问题覆盖**——Concept.md 中每个"要解决的问题"在 Plan.md 中是否有对应的功能或方案？
2. **洞察体现**——Concept.md 中的"深层洞察"是否在 Plan.md 的设计思考中得到回应？
3. **方案一致**——Concept.md 的"方案理由"与 Plan.md 的技术选型是否吻合？

如有遗漏或不一致之处，先补充 Plan.md，再展示给用户确认。

完成后，输出文档并告知用户确认。提示：「请确认 Plan.md 的内容。确认后告诉我「进入下一阶段」，我会继续生成 Project.md。」

---

## 阶段三：实施拆分 → Project.md

**目标**：把 Plan.md 中的规划拆分成可执行的阶段和任务，每个阶段有明确的交付结果和验收标准。

**前置条件**：项目根目录存在完整的 Plan.md。如不存在，提示用户应完成前置阶段。

### Project.md 模板

```markdown
# [项目名称] - 实施计划

## 阶段总览

<!-- 根据 Plan.md 产品形态调整：Web=前端骨架→后端→交互集成→部署；CLI=后端→部署；静态网站=前端（无交互集成）→部署 -->
本项目按 前端（界面骨架）→ 后端 → 前端（交互集成）→ 部署 四个阶段执行。

## 阶段详情

### 阶段一：前端（界面骨架）
- **任务列表**：
  - [ ] 设计系统规范——Design.md（颜色/字体/间距/组件库 tokens）
  - [ ] 视觉 Demo/原型（真实布局 + mock 数据）
- **交付结果**：<!-- 前端界面骨架完成，静态页面可浏览 -->
- **验收标准**：<!-- 页面渲染正常，与 Design.md 一致 -->

### 阶段二：后端
- **任务列表**：
  - [ ] 数据模型——DataModel.md（Schema/迁移/Model 定义）
  - [ ] API 设计——API.md（路由/端点/类型/校验）
  - [ ] 业务逻辑（Service/认证/集成联调）
- **交付结果**：<!-- 后端服务可运行，API 可用 -->
- **验收标准**：<!-- 端点返回正确，认证流程正常 -->

### 阶段三：前端（交互集成）
- **任务列表**：
  - [ ] 交互实现（状态管理/路由/API 集成）
- **交付结果**：<!-- 完整前端，接入真实 API -->
- **验收标准**：<!-- 核心用户流程可走通，前后端联调正常 -->

### 阶段四：部署
- **任务列表**：
  - [ ] CI/CD 配置
  - [ ] 上线
- **交付结果**：<!-- 项目已上线可访问 -->
- **验收标准**：<!-- 部署后可正常访问，核心功能可用 -->
```

- 根据 Plan.md 中的产品形态做适应性调整。例如 CLI 工具可跳过前端阶段（只需后端 + 部署），静态网站可跳过后端阶段（只需前端骨架 + 部署，无交互集成）。
- **简化模板**：对于落地页/品牌官网等简单前端项目（页面少、无复杂交互、无复用组件），前端阶段可移除"设计系统规范→Design.md"任务，直接从视觉 Demo 开始。样式和组件规范在实现中按需确定即可。
- 每个阶段应该是逻辑上自洽的工作单元，可以独立交付和验收。
- 任务粒度要适中：一个任务应可在 1-3 天内完成。
- 阶段之间如有依赖关系，需明确标注。

**状态管理**：每完成一个阶段，将 `[ ]` 更新为 `[x]`，并标注完成日期。

### 对齐校验：Project.md → Plan.md

生成 Project.md 后，对照 Plan.md 逐一检查：

1. **功能覆盖**——Plan.md 中的每个"核心功能"在 Project.md 中是否有对应的任务或阶段？
2. **技术栈一致**——Project.md 的技术选型是否与 Plan.md 保持一致？
3. **产品形态匹配**——Project.md 的阶段划分是否符 Plan.md 定义的产品形态（如 CLI 工具不应有前端阶段）？

如有遗漏或不一致之处，先补充 Project.md，再展示给用户确认。

完成后，输出文档并告知用户确认。提示：「请确认 Project.md 的内容。确认后告诉我「进入下一阶段」，进入任务跟踪阶段。」

---

## 阶段四：任务跟踪 → Task.md

**目标**：在实施阶段跟踪每个任务的执行状态，记录实施日志。

**前置条件**：项目根目录存在完整的 Project.md。如不存在，提示应完成前置阶段。

### Task.md 格式

```markdown
# [项目名称] - 任务跟踪

## 阶段一：[阶段名称]

### 任务 1：[任务名称] ✅ 已完成
#### 实施日志
- [2024-01-01] 做了什么，结果如何
- [2024-01-02] 遇到了什么问题，如何解决

### 任务 2：[任务名称] 🔄 进行中
#### 实施日志
- [2024-01-03] 开始实施，当前进展

### 任务 N：[任务名称] ⬜ 未开始
#### 实施日志
<!-- 暂无 -->

## 阶段二：[阶段名称]
<!-- 同上 -->
```

**状态管理规则**：
- 任务状态分三种：⬜ 未开始 / 🔄 进行中 / ✅ 已完成
- 每个任务应是可独立验证的单元
- 当某个阶段的所有任务验收通过后：
  1. 回到 **Project.md** 更新对应阶段的完成状态（`[ ]` → `[x]`），标注完成日期
  2. 读取 **Project.md** 确认下一阶段的任务列表
- 每次有进展时，在 Task.md 中追加日志，而不是覆盖

### 对齐校验：Task.md → 实现执行

进入阶段五前，对照 Project.md 检查：

1. **任务一致**——Task.md 中的任务列表是否与 Project.md 的阶段划分和任务定义一致？
2. **依赖满足**——是否有任务的前置依赖还未就绪？
3. **阶段状态**——各阶段的状态标记是否正确反映当前进展？

校验通过后，再开始阶段五的实现工作。

---

## 阶段五：实现执行 → 实际代码/项目

**目标**：基于 Project.md 的任务划分和 Task.md 的状态跟踪，按 Project.md 定义的阶段顺序（典型为 前端骨架 → 后端 → 前端交互集成 → 部署）实际创建项目、编写代码、部署上线。

**前置条件**：项目根目录存在完整的 Project.md 和 Task.md。如不存在，提示应先完成前置阶段。

### 执行总纲

1. **读取 Project.md** 获取阶段划分和各阶段的任务列表，确定产品形态
2. **根据 Project.md 定义的阶段顺序逐一实现**，典型 Web 项目按 前端（骨架）→ 后端 → 前端（交互集成）→ 部署 的顺序。具体阶段以 Project.md 为准（如静态网站可能只含前端骨架 + 部署，CLI 工具可能只含后端 + 部署）
3. **项目初始化**：进入第一个代码实现子步骤前，先初始化实际项目——创建项目目录、运行框架脚手架命令、安装依赖、初始化 git 仓库。如项目根目录已有项目代码或配置文件，跳过此步骤。
4. **需要规范文档的子步骤**（如存在：设计系统规范→Design.md、数据模型→DataModel.md、API 设计→API.md）：
   - 首先生成对应的**规范文档**，请求用户确认
   - 用户确认后，进入该子步骤的**代码实现**
   - 代码实现完成后更新 Task.md，执行对应产品形态的验证
   - 其他子步骤（视觉 Demo、交互集成、CI/CD、上线）无需规范文档，直接进入代码实现
5. **每个 Task 完成后**：
   - 更新 Task.md 中的任务状态（⬜ → ✅ 或 🔄）并追加实施日志
   - 执行对应产品形态的验证（见下方验证策略表）
   - 输出当前进展给用户确认
6. **对齐约束**：每个子步骤的代码实现必须对齐前置的规范文档（见各子步骤中的"对齐校验"要求）
7. **遇到计划外变更**（如技术方案不可行、需求矛盾）：停下来询问用户，获得指示后再继续
8. **一个阶段的所有 Task 全部完成后**：
   1. 回到 **Project.md**，将该阶段标记为 ✅（`[ ]` → `[x]`），标注完成日期
   2. 重新读取 **Project.md** 获取下一阶段的任务列表
   3. 请求用户确认是否进入下一阶段
   4. 确认后：
      - 如有下一阶段 → 跳转到该阶段继续执行
      - 如所有阶段已完成 → 进入阶段六（Summary.md）

### 5.1 前端（界面骨架）

先完成界面骨架（规范 + 静态页面），不依赖后端 API。交互实现在后端完成后单独进行（见 5.3）。

按照 **Design.md（设计系统规范）→ 视觉 Demo/原型** 的顺序执行。

---

#### 5.1.1 设计系统规范 → Design.md

**目标**：定义项目的视觉语言和组件规范，作为所有前端实现的唯一参照标准。

**生成 Design.md**，包含以下内容：

```markdown
# [项目名称] - 设计系统规范

## 设计 Tokens
- **色彩**：主色、辅色、中性色、语义色（成功/警告/错误），附色值
- **字体**：字族、字号层级、行高、字重
- **间距**：间距刻度（4px 基准）、内边距/外边距规则
- **阴影**：层级（卡片/下拉/弹窗）
- **圆角**：刻度（小/中/大/全圆）
- **动画**：缓动函数、持续时间

## 组件库
- **基础组件**：Button、Input、Select、Card、Modal、Tag、Toast 等
- **每个组件**：用途说明、样式属性、交互状态（hover/active/disabled）、尺寸变体

## 布局规范
- 页面网格、断点、响应式规则
- 内容区域最大宽度、边距

## 图标与资源
- 图标风格、来源（如 lucide-react 等）
```

**对齐校验：Design.md → Plan.md**
- Design.md 的设计风格是否与 Plan.md 中定义的风格一致？
- 如 Plan.md 中指定了 UI 框架（如 Tailwind/shadcn），Design.md 是否一致？

完成后输出文档给用户确认。提示：「请确认 Design.md 的设计规范。确认后进入视觉 Demo 阶段。」

**后续约束**：视觉 Demo 必须严格对齐 Design.md 中的所有 tokens 和组件规范。如有偏离，先修改 Design.md（记录 Change.md）再统一调整。5.3 交互集成也需对齐 Design.md。

---

#### 5.1.2 视觉 Demo/原型

**对齐 Design.md**：使用的颜色、字体、间距、组件必须与 Design.md 定义一致。

此阶段使用 **design-taste-frontend**（taste-skill 的反 slop 前端技能）指导视觉实现。执行以下流程：

##### A. Design Read（设计解读）
开始编码前，输出一行设计解读：
> **"解读为：[页面类型]，面向[受众]，[风格]语言，倾向[技术栈/设计系统]"**

示例：
- *"解读为：B2B SaaS Landing Page，面向技术采购者，Linear 风格极简语言，倾向 Tailwind + Geist + 克制动效"*
- *"解读为：个人作品集首页，面向招聘方和客户，现代极简语言，倾向自定义 CSS + 克制动效"*

如用户需求不明确，只问**一个**澄清问题，不批量提问。

##### B. 三旋钮设定
根据设计解读设定三个 1-10 旋钮：

| 旋钮 | 说明 | 典型值 |
|------|------|--------|
| **DESIGN_VARIANCE** | 1=完全对称 → 10=艺术化非对称 | 个人作品集 6-8 |
| **MOTION_INTENSITY** | 1=静态 → 10=电影级动效 | 标准 Landing Page 4-7 |
| **VISUAL_DENSITY** | 1=画廊级留白 → 10=仪表盘级紧凑 | 营销页面 3-5 |

默认基线：`VARIANCE: 7 / MOTION: 6 / DENSITY: 4`

常见场景预设：

| 场景 | VARIANCE | MOTION | DENSITY |
|------|----------|--------|---------|
| SaaS Landing Page | 7 | 6 | 4 |
| 创意机构首页 | 9 | 8 | 3 |
| 开发者个人作品集 | 6 | 5 | 4 |
| 设计师个人作品集 | 8 | 7 | 3 |
| 电商/品牌首页 | 7 | 6 | 3 |

##### C. 设计系统选择
根据 Design.md 和 Plan.md 选择合适的设计系统基础：

| 项目类型 | 推荐方案 |
|---------|---------|
| 现代 SaaS / AI 营销 | Tailwind v4 + shadcn/ui |
| 企业级 B2B | IBM Carbon / Fluent UI |
| 极简个人网站 | 自定义 CSS + Tailwind 工具类 |
| 博客 / 编辑类 | 自定义 CSS，强调排版 |
| 公共部门 | GOV.UK Frontend / USWDS |

##### D. 反 AI Slop 规则
视觉 Demo 必须遵循以下约束（来自 taste-skill）：

**布局：**
- 避免默认的居中 Hero + 紫色渐变（如非品牌要求）
- 禁止三张等宽功能卡并列
- 禁止 `h-screen`（用 `min-h-[100dvh]`）
- 禁止使用 em-dash（`—`），全部用普通连字符（`-`）
- Hero 必须在首屏完整可见，标题 ≤ 2 行，副文本 ≤ 20 词
- CTA 按钮文字必须在桌面端单行显示

**字体：**
- 避免 Inter 作为默认字体。优先 Geist、Satoshi、Instrument Sans 等
- 展示/标题字重用 `tracking-tighter leading-none`
- 正文字重用 `leading-relaxed max-w-[65ch]`

**色彩：**
- 最多 1 个强调色，饱和度 < 80%
- 避免 AI 默认的紫色/蓝色渐变
- 全页保持单一强调色，不混用

**卡片与间距：**
- 卡片只在使用层级时使用，否则用 `border-t` / `divide-y` 分组
- 圆角体系全页统一（全锐/全圆/全柔和，选一种）
- Hero 不接受纯文字 + 渐变色块——必须有真实视觉元素

**动效（MOTION_INTENSITY > 3 时）：**
- 只动 `transform` 和 `opacity`，不动 top/left/width/height
- 必须尊重 `prefers-reduced-motion`
- 禁止 `window.addEventListener('scroll')`
- 滚动触发动画使用 IntersectionObserver 或 CSS `animation-timeline`

##### E. 实现
用真实布局和 mock/静态数据构建页面：
- 不接入真实 API
- ✅ 验证：页面无报错渲染，响应式布局正常，链接可导航

##### F. 预检清单（Pre-Flight Check）
交付前逐项检查：

- [ ] 设计解读已声明（步骤 A）
- [ ] 旋钮值与需求匹配（步骤 B）
- [ ] 无 AI 默认色（紫/蓝渐变）
- [ ] 无 em-dash
- [ ] 全页色彩一致性锁定
- [ ] 全页圆角体系一致性锁定
- [ ] Hero 首屏完整可见，CTA 无需滚动
- [ ] CTA 按钮文字在桌面端不换行
- [ ] Hero 文本元素 ≤ 4 个（eyebrow/标题/副文本/CTA）
- [ ] CTA 无重复意图（同一页面不同位置的 CTA 文案一致）
- [ ] 动效尊重 `prefers-reduced-motion`
- [ ] 导航单行显示，高度 ≤ 80px
- [ ] 响应式检查：768px 断点布局正常
- [ ] Z-index 有层次管理，不随意使用 z-50

**对齐校验：视觉 Demo → Design.md**
- 页面使用的全部色彩值是否在 Design.md 的 tokens 范围内？
- 组件使用方式是否符合 Design.md 中的组件规范？
- 间距、圆角、阴影是否严格遵循 Design.md 的刻度？

如有偏离，需调整代码对齐 Design.md。

完成后输出进展给用户确认。

---

### 5.2 后端实现

按照 **DataModel.md（数据模型）→ API.md（API 设计）→ 业务逻辑实现** 的顺序执行。每个子步骤完成后请求用户确认，后续步骤必须对齐前置规范文档。

---

#### 5.2.1 数据模型 → DataModel.md

**目标**：定义完整的数据库 Schema 作为后端实现的唯一数据参照标准。

**生成 DataModel.md**，包含以下内容：

```markdown
# [项目名称] - 数据模型

## 实体定义
### [实体名称]
- **字段**：字段名 / 类型 / 约束（如 NOT NULL、UNIQUE、DEFAULT）
- **主键**：<!-- 主键字段 -->
- **索引**：<!-- 索引字段列表 -->
- **约束**：<!-- 字段级业务约束，如 金额 > 0、状态 ∈ {active, archived} -->
- **关系**：
  - belongs_to：[关联实体]
  - has_many：[关联实体]

## 关系图（文字描述）
<!-- 实体间的一对多、多对多关系 -->

## 数据流说明
<!-- 核心数据的生命周期和流转路径 -->

## 业务规则
<!-- 跨实体的业务约束和操作规则 -->
- **[规则 1]**：<!-- 如：删除分类前需确认该分类下无关联交易 -->
- **[规则 2]**：<!-- 如：预算金额不可超过设定的收入上限 -->
```

**对齐校验：DataModel.md → Plan.md**
- 实体定义是否覆盖了 Plan.md 核心功能所需的数据？
- 数据模型复杂度是否与项目规模匹配（如简单的记账应用不需要多租户设计）？

完成后输出文档给用户确认。提示：「请确认 DataModel.md 的数据模型。确认后进入 API 设计阶段。」

**后续约束**：API 设计和业务逻辑实现必须严格对齐 DataModel.md 中的实体和关系定义。

---

#### 5.2.2 API 设计 → API.md

**对齐 DataModel.md**：API 的请求/响应结构必须基于 DataModel.md 中的实体定义。

**生成 API.md**，包含以下内容：

```markdown
# [项目名称] - API 设计

## 基础信息
- **Base URL**：`/api/v1`
- **认证方式**：<!-- JWT / Session / API Key -->
- **数据格式**：JSON

## 端点列表

### [资源名]
- `GET /[资源]` — 列表查询，参数：<!-- 分页/筛选 -->
- `POST /[资源]` — 创建，请求体：<!-- 字段列表 -->
- `GET /[资源]/:id` — 详情
- `PUT /[资源]/:id` — 更新，请求体：<!-- 字段列表 -->
- `DELETE /[资源]/:id` — 删除

## 数据结构
<!-- 主要请求/响应的 JSON 结构示例 -->

## 错误码
<!-- 通用错误码定义 -->
```

**对齐校验：API.md → DataModel.md**
- 每个端点的数据字段是否能在 DataModel.md 中找到对应实体和属性？
- API 中的关系引用是否与 DataModel.md 中定义的一致？

完成后输出文档给用户确认。提示：「请确认 API.md 的接口设计。确认后进入业务逻辑实现阶段。」

**后续约束**：业务逻辑实现必须严格对齐 API.md 中的端点定义；前端交互实现中的 API 调用也必须对齐 API.md。

---

#### 5.2.3 业务逻辑实现

**对齐 DataModel.md**：数据操作必须遵循 DataModel.md 的 Schema 定义。
**对齐 API.md**：实现的接口必须与 API.md 的端点定义一致。

- 实现 Service 层：认证/授权、校验规则、核心业务
- 后端服务可独立运行和测试
- ✅ 验证：集成测试通过，认证流程正确，关键业务路径可工作

**对齐校验：业务逻辑 → DataModel.md + API.md**
- 数据读写是否与 DataModel.md 中的字段和约束一致？
- 暴露的端点是否与 API.md 的定义完全匹配？
- 返回的数据结构是否与 API.md 一致？
- 业务校验逻辑是否与 DataModel.md 中的约束和业务规则一致？

完成后输出进展给用户确认。

### 5.3 前端交互集成

后端实现完成后，回到前端接入真实 API，完成完整交互。

**对齐 Design.md**：所有 UI 必须符合设计系统规范。
**对齐 API.md**：API 调用必须符合后端定义的端点和数据结构。

- 接入状态管理、路由、真实 API
- 实现表单处理、用户操作、数据展示
- ✅ 验证：核心用户流程可走通，前后端联调正常

**对齐校验：交互实现 → Design.md + API.md**
- UI 组件和样式是否仍与 Design.md 保持一致（如有新增组件，先在 Design.md 补充定义）？
- API 调用是否与 API.md 中的端点定义一致？

完成后输出进展给用户确认。

---

### 5.4 部署

按照 CI/CD → 上线的顺序执行。每个子步骤完成后输出进展给用户确认。

**1. CI/CD 配置**
- 配置 CI 流水线（GitHub Actions 或其他）：自动构建、运行测试
- 配置自动化部署触发（push 到主分支等）
- ✅ 验证：流水线可成功触发并完整运行

完成后输出进展给用户确认。

**2. 上线**
- 配置托管平台（Vercel / Railway / Fly.io 等）
- 设置环境变量、密钥、数据库连接
- 配置域名（如需要）、SSL 证书
- ✅ 验证：部署后可正常访问，核心功能可用

完成后输出进展给用户确认。

### 验证策略表

根据 Plan.md 中的产品形态选择对应的验证方式：

| 产品形态 | 验证方式 |
|---------|---------|
| Web 应用（前后端分离/全栈） | 启动 dev server → 页面 200 → 核心交互可操作 |
| 静态网站 / Landing Page | 启动 dev server → 页面渲染 → 链接可达 |
| API / 后端服务 | 启动 server → curl 关键端点 → 返回值和状态码正确 |
| CLI 工具 | 直接执行命令 → 核验 stdout/stderr 输出 |
| npm 包 / 库 | 运行测试套件 |
| 浏览器扩展 | 代码审查 + 标记「需手动加载验证」 |
| 混合型 / 其他 | 组合上述方式，覆盖各部分的验证 |

### 实现阶段输出

- **项目代码文件**（根据具体技术栈创建）— 这是主要的产出
- **规范文档**（仅相关子步骤生成）：
  - `Design.md` — 设计系统规范（前端）
  - `DataModel.md` — 数据模型定义（后端）
  - `API.md` — API 接口定义（后端）
  - 这些规范文档是对齐的基准，后续步骤必须遵守
- **Task.md 更新** — 每个任务完成后更新状态和日志
- **Project.md 更新** — 每阶段完成后更新进度
- **Change.md 记录** — 如有方案变更或规范文档修改，记录变更原因和决策过程

---

## 变更管理 → Change.md

**核心规则**：任何时候覆盖重写以下文档，都必须在 Change.md 中追加一条记录：Concept.md、Plan.md、Project.md、Design.md、DataModel.md、API.md。

### Change.md 格式

```markdown
# 变更日志

## 2024-01-01 14:30
- **修改文档**：Concept.md
- **修改内容**：更新了问题定义，从"用户找不到附近的餐厅"改为"用户无法高效比较附近餐厅的评分和价格"
- **修改原因**：用户反馈原问题定义过于宽泛，聚焦后更精准

## 2024-01-02 10:00
- **修改文档**：Plan.md
- **修改内容**：技术栈从 React + Express 改为 Next.js 全栈
- **修改原因**：简化部署架构，减少维护成本
```

- Change.md 不会被覆盖，只会追加。
- 每次修改都记录：时间、文档、内容、原因。
- Task.md 的更新日志和 Summary.md 不需要在 Change.md 中记录。

---

## 阶段六：项目总结 → Summary.md

**触发条件**：所有开发阶段完成，所有任务验收通过后，或用户主动要求总结时。

### Summary.md 模板

```markdown
# [项目名称] - 项目总结

## 项目概况
<!-- 简要回顾项目目标 -->

## 完成情况
- **已完成功能**：<!-- 列表 -->
- **未完成/延期功能**：<!-- 列表及原因 -->

## 关键决策记录
<!-- 开发过程中做出的关键决策，参考 Change.md -->

## 经验总结
<!-- 项目中做得好的和可以改进的地方 -->

## 后续建议
<!-- 对后续维护/迭代的建议 -->
```

---

## 阶段切换规则

🛑 STOP：这是所有阶段共用的出口守则，任何阶段结束后必须按此流程执行，不可跳过。

每阶段结尾的行为是固定的：

1. **展示文档** — 把刚生成的文档完整输出给用户看
2. **交叉校验** — 对照前序文档检查内容对齐（见各阶段末尾的校验清单），如有遗漏先补充
3. **请求确认** — 询问用户是否满意，或者需要修改
4. **如果是修改**：修改文档 → 记录 Change.md → 重新展示
5. **如果确认通过**：提示用户手动触发下一阶段（"请告诉我「进入下一阶段」继续"）
6. **等待用户指令**：不要自动进入下一阶段

## 常见场景处理

| 场景 | 处理方式 |
|------|---------|
| 用户直接提供了 Concept.md | 审查完整性，对照 6 个问题进行评估，补充缺失项 |
| 用户想跳过某个阶段 | 允许跳过，但说明跳过后可能影响后续文档质量 |
| 用户要求修改已完成的文档 | 直接修改对应文档，并在 Change.md 中记录 |
| 项目根目录已经有部分文档 | 检查已有文档，从缺失的阶段开始 |
| 多个阶段文档同时存在 | 检查一致性，如有冲突则在 Change.md 中记录并优先使用最新版本 |
| 用户之前用过此 Skill 但中断了 | 检查项目根目录的文档状态，从中断的阶段继续 |
| 用户要求修改已实现的代码 | 先在 Change.md 记录变更原因，然后修改代码，更新 Task.md 状态 |
| 规范文档（Design/DataModel/API.md）与代码不一致 | 先更新规范文档（记录 Change.md 说明修改原因），再统一调整所有对齐的代码 |

## 操作反例黑名单

以下反模式会在使用本技能时导致文档不一致、项目混乱或 Agent 接力失败。执行任何阶段前对照检查：

| # | 反模式 | 后果 | 正确做法 |
|---|--------|------|---------|
| 1 | 不经过用户确认直接进入下一阶段 | 用户失去对项目方向的控制，后续文档可能偏离意图 | 每阶段完成后必须按「阶段切换规则」执行：展示→校验→确认→等待指令 |
| 2 | 跳过多平台校验直接实现 | 不同 Agent 接力时无法理解当前进度，可能导致重复工作 | 每次回到项目先 `ls` 检查根目录文档状态，从现有文档继续 |
| 3 | 直接修改已确认的文档而不记录 Change.md | 后续 Agent 无法追踪变更原因，决策链断裂 | 每次覆盖重写 Concept/Plan/Project/Design/DataModel/API.md 前，先在 Change.md 追加记录变更原因 |
| 4 | 同时修改代码和规范文档 | 无法区分代码改动和文档改动，交叉校验失效 | 先改规范文档（记录 Change.md），再统一调整所有对齐的代码 |
| 5 | 跳过交叉校验直接输出文档 | 前后阶段文档不一致，项目后期发现矛盾需要重做 | 每阶段输出后，先对照前序文档的校验清单检查，再展示给用户 |
| 6 | 多个文档同时修改不标注顺序 | 变更互相覆盖，Agent 接力时不知道哪个版本是最终版 | 一次只改一个文档，修改后立即展示给用户确认再改下一个 |
| 7 | 用户说「继续」就自动批量推进多个阶段 | 用户来不及做决策就被推着走，反悔成本高 | 每阶段展示→确认→等待指令，一次只推进一个阶段（除非用户明确要求「跳过确认一下子做完」） |

## 工具使用

- 平台自动提供标准的**文件读写能力**（读、写、编辑、执行命令），无需在实现中指定具体工具名
- 使用 `ls` 或平台自带命令检查项目根目录的文件状态
- 生成文档时直接描述要创建的文件内容，平台会自动执行写入操作
- 记录 Change.md 时同理
